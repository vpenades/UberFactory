<UserControl x:Class="Epsylon.UberFactory.Themes.ProjectItems.PipelineView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Epsylon.UberFactory.Themes.ProjectItems"
             mc:Ignorable="d" >    
    
    <Grid>

        <ContentControl Content="{Binding Content}">
            <ContentControl.ContentTemplate>
                <DataTemplate>
                    <local:NodeView />
                </DataTemplate>
            </ContentControl.ContentTemplate>
        </ContentControl>

        <Button Content="{StaticResource _AddIcon}" Command="{Binding ChooseParameterCmd}"
                Visibility="{Binding IsEmpty,    Converter={StaticResource asVisible}}"/>        

    </Grid>
    

</UserControl>
