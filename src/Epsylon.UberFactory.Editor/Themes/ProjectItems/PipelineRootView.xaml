<UserControl x:Class="Epsylon.UberFactory.Themes.ProjectItems.PipelineRootView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Epsylon.UberFactory.Themes.ProjectItems"
             xmlns:themes="clr-namespace:Epsylon.UberFactory.Themes"             
             mc:Ignorable="d">

    <UserControl.Resources>
        <themes:TypeDisplayNameConverter x:Key="_AsDisplayName" />
    </UserControl.Resources>

    <GroupBox Style="{StaticResource VerticalGroupBoxStyle}">

        <GroupBox.Header>
            <WrapPanel HorizontalAlignment="Center">                

                <GroupBox Style="{StaticResource HorizontalGroupBoxStyle}"
                              Header="{Binding Pipeline.Content.DisplayName}"
                              Visibility="{Binding Pipeline.IsInstanced,Converter={StaticResource asVisible}}" >

                    <themes:SafeButton Content="{StaticResource _RemoveIcon}" HorizontalAlignment="Center" Command="{Binding Pipeline.ClearCmd}" />
                </GroupBox>

            </WrapPanel>
        </GroupBox.Header>

        <local:PipelineView DataContext="{Binding Pipeline}" />

    </GroupBox>
    
</UserControl>
